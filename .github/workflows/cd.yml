name: CD

run-name: >
  Publish → ${{ github.event.workflow_run.name || format('Manual Publish {0}', github.run_number) }}

on:
  workflow_run:
    workflows:
      - "CI"
    types:
      - completed
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy application
        run: echo "Deploying application..."
